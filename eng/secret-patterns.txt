# One case-insensitive regular expression per line. These patterns are used
# only against release candidates; they are not a substitute for redaction.
Bearer\s+[A-Za-z0-9._~+/=-]{16,}
refresh[_-]?token\s*[:=]\s*[^\s"']{12,}
client[_-]?secret\s*[:=]\s*[^\s"']{8,}
-----BEGIN\s+(RSA|EC|OPENSSH|PRIVATE)\s+KEY-----
password\s*[:=]\s*[^\s"']{8,}
oauth[_-]?code\s*[:=]\s*[^\s"']{8,}
