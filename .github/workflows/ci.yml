name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  verify-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - name: Verify repository text line endings
        shell: pwsh
        run: |
          $bad = git ls-files --eol | Select-String 'w/crlf'
          if ($bad) {
            $bad | ForEach-Object { Write-Error $_.Line }
            exit 1
          }
      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json
          cache: true
          cache-dependency-path: '**/packages.lock.json'
      - name: Verify
        shell: pwsh
        run: ./eng/verify.ps1
      - name: Verify local configuration and release tooling
        shell: pwsh
        run: |
          ./eng/tests/entra-registration.tests.ps1
          ./eng/tests/release-tooling.tests.ps1
